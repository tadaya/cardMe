<!DOCTYPE html>
<html>
<head>
  <title>Cardme</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>

  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  
  <% unless @user.nil? %>
    <script>localStorage["user_id"] = <%= @user.id %></script>
    <!-- localStorage hash is a storage for manipulation with variables from different languages and stuff like that. DmitryJ.-->
  <% end %>
</head>
<body>

<header>
  <ul class="nav">

    <li><%= link_to("Log In", "/" ) if session[:user_id].nil? %></li>
    <li><%= link_to "Profile", user_path(session[:user_id]) unless session[:user_id].nil?%></li>
    <li>
     <%= link_to("Log Out, #{current_user.first_name}", "/logout", method: "delete") unless session[:user_id].nil? %>
   </li>
  </ul>
</header>


<%= yield %>

</body>
</html>
